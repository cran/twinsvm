% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsvm.R
\name{tsvm}
\alias{tsvm}
\title{Fit a Twin Support Vector Machine}
\usage{
tsvm(
  x,
  y,
  method = c("ls", "twin"),
  kernel = c("linear", "rbf", "poly"),
  c1 = 1,
  c2 = 1,
  gamma = NULL,
  degree = 3,
  coef0 = 1,
  eps = 1e-06
)
}
\arguments{
\item{x}{Numeric matrix or data frame of predictors.}

\item{y}{Response with at least two classes.}

\item{method}{Twin-SVM method. \code{"ls"} fits least-squares twin SVM;
\code{"twin"} fits the original box-constrained dual formulation.}

\item{kernel}{Kernel name.}

\item{c1, c2}{Positive regularization parameters.}

\item{gamma}{Kernel scale. Defaults to \code{1 / ncol(x)}.}

\item{degree}{Polynomial degree.}

\item{coef0}{Polynomial offset.}

\item{eps}{Ridge term added to every linear solve.}
}
\value{
A fitted \code{tsvm} object for two classes, or a \code{tsvm_multiclass} object
for three or more classes.
}
\description{
Fits a twin support vector machine. With two classes this uses the validated
binary twin-SVM path: level 1 of \code{y} is class B, level 2 is class A, plane 1
is close to class A, and plane 2 is close to class B. With three or more
classes, the function fits one binary twin SVM for each class pair and
predicts by majority vote. Multiclass ties are resolved by choosing the class
that appears first in the factor level order.
}
\examples{
set.seed(3)
dat <- gen_moons(50, noise = 0.05)
fit <- tsvm(dat$x, dat$y)
predict(fit, dat$x[1:4, ])
}
\references{
Jayadeva, Khemchandani, R., and Chandra, S. (2007). Twin support vector
machines for pattern classification. \emph{IEEE Transactions on Pattern Analysis
and Machine Intelligence}, 29(5), 905-910.

Kumar, M. A. and Gopal, M. (2009). Least squares twin support vector
machines for pattern classification. \emph{Expert Systems with Applications},
36(4), 7535-7543.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{cv_tsvm}
\alias{cv_tsvm}
\title{Cross-Validate Twin-SVM Hyperparameters}
\usage{
cv_tsvm(x, y, c1_grid, c2_grid, gamma_grid = NULL, k = 5, ...)
}
\arguments{
\item{x}{Numeric matrix or data frame of predictors.}

\item{y}{Two-class response.}

\item{c1_grid, c2_grid}{Positive numeric vectors.}

\item{gamma_grid}{Optional positive numeric vector. If \code{NULL}, \code{gamma} is
left at the \code{tsvm()} default.}

\item{k}{Number of folds.}

\item{...}{Additional arguments passed to \code{\link[=tsvm]{tsvm()}}.}
}
\value{
A \code{cv_tsvm} object with \code{best_params} and \code{results}.
}
\description{
Runs k-fold cross-validation over \code{c1}, \code{c2}, and optionally \code{gamma}.
}
\examples{
set.seed(10)
dat <- gen_moons(40, noise = 0.1)
cv <- cv_tsvm(dat$x, dat$y, c1_grid = c(0.1, 1), c2_grid = c(0.1, 1), k = 3)
cv$best_params
}

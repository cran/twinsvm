% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{resample_accuracy}
\alias{resample_accuracy}
\title{Accuracy and Its Spread over Repeated Random Splits}
\usage{
resample_accuracy(
  x,
  y,
  model = c("both", "tsvm", "svms"),
  reps = 50L,
  prop = 0.7,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric matrix or data frame of predictors.}

\item{y}{Response factor with at least two classes.}

\item{model}{Which classifier(s) to evaluate: \code{"both"} (the default,
\code{\link[=tsvm]{tsvm()}} and \code{\link[=svms]{svms()}} on the same splits), \code{"tsvm"}, or \code{"svms"}.}

\item{reps}{Number of random splits (at least 2; the spread is estimated
across them).}

\item{prop}{Proportion of observations used for training in each split,
between 0 and 1.}

\item{seed}{Optional integer seed. Set it for reproducible splits; because
the splits are fixed before any model is fitted, the comparison is stable
even though the SMO solver in \code{\link[=svms]{svms()}} itself draws random numbers.}

\item{...}{Further arguments passed to the fitter(s), e.g. \code{kernel},
\code{gamma}, \code{cost} (standard SVM), or \code{c1}, \code{c2}, \code{method} (twin SVM). Each
argument is forwarded only to the fitter(s) that accept it, so a single
call such as \verb{kernel = "rbf", gamma = 1} works for \code{model = "both"}.}
}
\value{
An object of class \code{resample_accuracy}, a list with
\describe{
\item{\code{summary}}{a data frame with one row per model giving \code{mean}, \code{sd},
\code{se} and \code{n} of the test accuracy;}
\item{\code{accuracy}}{a long data frame of the per-split accuracies
(\code{model}, \code{rep}, \code{accuracy});}
\item{\code{reps}, \code{prop}, \code{models}, \code{call}}{the settings used.}
}
The \link[=plot.resample_accuracy]{plot()} method draws the accuracy
distributions as boxplots.
}
\description{
Evaluates a classifier by repeated random holdout: the data are split into
training and test sets \code{reps} times, the model is refitted on each training
set, and test accuracy is recorded. Reporting the mean \strong{together with its
between-split standard deviation and standard error} turns a single accuracy
number into an estimate with a stated uncertainty, so that differences
between methods can be read against the noise rather than taken at face
value.
}
\details{
The splits are drawn once, up front, and shared by every model, so when
\code{model = "both"} the twin SVM and the standard SVM are compared on identical
training and test sets (a paired comparison). Splits are stratified by class,
so each class keeps roughly its overall proportion in both parts.
}
\examples{
set.seed(1)
dat <- gen_moons(120, noise = 0.15)
rs <- resample_accuracy(dat$x, dat$y, model = "both",
                        reps = 10, kernel = "rbf", gamma = 1, seed = 1)
rs                 # mean, sd and se per model
plot(rs)           # accuracy spread as boxplots
}

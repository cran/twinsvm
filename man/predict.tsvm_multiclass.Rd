% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ovo.R
\name{predict.tsvm_multiclass}
\alias{predict.tsvm_multiclass}
\title{Predict from a Multiclass Twin SVM}
\usage{
\method{predict}{tsvm_multiclass}(object, newdata, type = c("class", "votes"), ...)
}
\arguments{
\item{object}{A fitted \code{tsvm_multiclass} object.}

\item{newdata}{Numeric matrix or data frame.}

\item{type}{Output type. \code{"class"} returns predicted class labels;
\code{"votes"} returns the vote matrix.}

\item{...}{Unused. \code{decision.values} is not supported for multiclass
objects because OVO decision values do not have a single unambiguous scale.}
}
\value{
A factor of predicted classes, or an integer vote matrix when
\code{type = "votes"}.
}
\description{
Predicts from a one-vs-one multiclass twin SVM. Each binary model votes for
one class. Ties are resolved deterministically by choosing the class that
appears first in the training factor level order.
}
\examples{
set.seed(40)
x <- rbind(
  matrix(rnorm(20, -2, 0.2), ncol = 2),
  matrix(rnorm(20, 0, 0.2), ncol = 2),
  matrix(rnorm(20, 2, 0.2), ncol = 2)
)
y <- factor(rep(c("a", "b", "c"), each = 10))
fit <- tsvm(x, y, kernel = "linear")
predict(fit, x[1:3, , drop = FALSE])
}
\seealso{
Other multiclass:
\code{\link[=confusion]{confusion()}},
\code{\link[=predict.svms_multiclass]{predict.svms_multiclass()}},
\code{\link[=print.svms_multiclass]{print.svms_multiclass()}},
\code{\link[=print.tsvm_multiclass]{print.tsvm_multiclass()}}
}
\concept{multiclass}

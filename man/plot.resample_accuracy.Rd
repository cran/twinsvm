% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{plot.resample_accuracy}
\alias{plot.resample_accuracy}
\title{Plot the Accuracy Spread from Repeated Splits}
\usage{
\method{plot}{resample_accuracy}(x, ...)
}
\arguments{
\item{x}{A \code{resample_accuracy} object.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the per-split test accuracies as a boxplot, one box per model, so the
dispersion is visible alongside the mean.
}
\examples{
set.seed(2)
dat <- gen_moons(120, noise = 0.15)
rs <- resample_accuracy(dat$x, dat$y, reps = 10, kernel = "rbf", gamma = 1)
plot(rs)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confusion.R
\name{confusion}
\alias{confusion}
\title{Confusion Matrix and Accuracy}
\usage{
confusion(object, x, y)
}
\arguments{
\item{object}{A fitted \code{tsvm}, \code{tsvm_multiclass}, \code{svms}, or
\code{svms_multiclass} object.}

\item{x}{Numeric matrix or data frame of predictors.}

\item{y}{True class labels.}
}
\value{
A list with \code{table}, a \code{table(truth, predicted)} confusion matrix,
and \code{accuracy}, the overall classification accuracy.
}
\description{
Predicts on \code{x} and returns a confusion matrix plus overall accuracy. This
works for both binary and one-vs-one multiclass \code{tsvm} and \code{svms} fits.
}
\examples{
set.seed(50)
x <- rbind(
  matrix(rnorm(20, -2, 0.2), ncol = 2),
  matrix(rnorm(20, 0, 0.2), ncol = 2),
  matrix(rnorm(20, 2, 0.2), ncol = 2)
)
y <- factor(rep(c("a", "b", "c"), each = 10))
fit <- tsvm(x, y, kernel = "linear")
confusion(fit, x, y)
}
\seealso{
Other multiclass:
\code{\link[=predict.svms_multiclass]{predict.svms_multiclass()}},
\code{\link[=predict.tsvm_multiclass]{predict.tsvm_multiclass()}},
\code{\link[=print.svms_multiclass]{print.svms_multiclass()}},
\code{\link[=print.tsvm_multiclass]{print.tsvm_multiclass()}}
}
\concept{multiclass}
